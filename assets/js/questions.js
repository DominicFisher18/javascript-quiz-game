const questions = [
    'Which of these is NOT a javascript operator?',
    'What does NaN stand for in javascript?',
    'What is the DOM?',
    'What is the difference between == and === ?',
    'What does the pop() method do in javascript?'
]

const A = [
    'string',
    'not a noun',
    'document object model',
    '== means true, === means false',
    'pops an extra value into an array'
]

const B = [
    'boolean',
    'not a number',
    'dom-inic',
    'they mean the same thing',
    'returns a popcorn emoji into the console'
]

const C = [
    'adjective',
    'not a name',
    'data object malfuction',
    'these operators are not in javascript',
    'returns the last element from an array'
]

const D = [
    'number',
    'no answer needed',
    'dont order malware',
    '== compares values, === compares types and values',
    'creates a popping sound when you click a button'
]

const correctAnswers = [
    'adjective',
    'not a number',
    'document object model',
    '== compares values, === compares types and values',
    'returns the last element from an array'
]